# k8s/services/claude-svc.yaml
apiVersion: v1
kind: Service
metadata:
  name: claude
  namespace: wolf-engine
  labels:
    app:                         wolf-engine
    app.kubernetes.io/name:      wolf-engine
    app.kubernetes.io/component: claude
spec:
  type: ClusterIP
  sessionAffinity: None
  selector:
    app.kubernetes.io/component: claude
    stage:                       stable
  ports:
    - name:       http
      port:       80
      targetPort: http
      protocol:   TCP
